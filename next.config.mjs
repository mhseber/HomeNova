// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn-icons-png.flaticon.com"], // ✅ তুমি যেই external image ইউজ করছো, সেই domain
  },
};

export default nextConfig;
